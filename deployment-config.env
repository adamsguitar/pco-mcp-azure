# Deployment configuration for GitHub Actions workflow
# This file is read by the workflow to determine Docker image settings
# These are NOT Terraform variables - they're used for Docker build/push only

container_image_name="pco-mcp"
container_image_tag="latest"
